{
    "compilerOptions": {
        "module": "commonjs",
        "baseUrl": ".",  // Correction ici (c'était "baseRul")
        "paths": {
            "@/*": ["./resources/js/*"]  // Correction ici (valeur mise dans un tableau)
        }
    },
    "exclude": ["node_modules"]
}
